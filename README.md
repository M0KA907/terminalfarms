# Terminal Farms

A terminal farming deskpet.

## Run

Requires Rust 1.88 or newer.

```bash
git clone https://github.com/M0KA907/terminalfarms.git
cd terminalfarms
cargo run --release
```

Press `q` to quit.
